# get the job portal application from git
git init
git clone https://github.com/OviYaD/JobApplication.git

# create a dockerfile, docker-compose file and nginx file to deploy that application
docker compose up --build






# to delete all use
docker compose down